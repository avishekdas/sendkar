apiVersion: v1
kind: Service
metadata:
  name: sendkar-kafka
  labels:
    name: sendkar-kafka
spec:
  ports:
  - port: 9092
    targetPort: 9092
  selector:
    role: sendkar-kafka
---
apiVersion: apps/v1beta1
kind: StatefulSet
metadata:
  name: sendkar-kafka
spec:
  serviceName: "kafka"
  replicas: 1
  template:
    metadata:
      labels:
        role: sendkar-kafka
    spec:
      terminationGracePeriodSeconds: 10
      containers:
        - name: sendkar-kafka
          image: eventuateio/eventuateio-local-kafka:0.14.0
          env:
            - name: KAFKA_HEAP_OPTS
              value: -Xmx320m -Xms320m
            - name: ZOOKEEPER_SERVERS
              value: sendkar-zookeeper:2181
          ports:
              - containerPort: 9092
          volumeMounts:
            - name: sendkar-kafka-persistent-storage
              mountPath: /usr/local/kafka-data
            - name: sendkar-kafka-config-volume
              mountPath: /usr/local/kafka-config
      volumes:
      - name: sendkar-kafka-config-volume
        configMap:
          name: sendkar-kafka-config
  volumeClaimTemplates:
  - metadata:
      name: sendkar-kafka-persistent-storage
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
---
